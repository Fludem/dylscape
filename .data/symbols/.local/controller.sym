# Controllers authored by this server. See `content.sym` for the id-allocation reasoning.
# Upstream `controller.sym` has a single entry (0), so 1000 leaves no realistic overlap.
#
# One controller instance per laid trap, keyed on the trap's tile. Controllers are
# name-only server types with no cache encoder, so this needs no `packCache`.
1000	hunter_trap
